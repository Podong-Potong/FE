import styled from "styled-components";
import { DayofWeek } from "../NoSpendMoney/style";

export const SaveMoney = styled(DayofWeek)<{ isClicked: boolean }>`
	border-radius: 25px;
	width: fit-content;
	padding: 8px;
`;
